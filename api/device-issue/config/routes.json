{
  "routes": [
    {
      "method": "GET",
      "path": "/device-issues",
      "handler": "device-issue.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/device-issues/count",
      "handler": "device-issue.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/device-issues/:id",
      "handler": "device-issue.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/device-issues",
      "handler": "device-issue.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/device-issues/:id",
      "handler": "device-issue.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/device-issues/:id",
      "handler": "device-issue.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
